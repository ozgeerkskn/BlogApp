<%- include ("partials/header.ejs") %>
<div>
  <div class="container login">
    <% if (locals.dataList && locals.dataList.length > 0) { %> <% for (let i =
    0; i < locals.dataList.length; i++) { %>
    <h3>@<%= locals.dataList[i].username %></h3>
    <h4><%= locals.dataList[i].topic %></h4>
    <p><%= locals.dataList[i].blog %></p>
    <hr />
    <% } %> <% } else { %>

    <p>There is no data yet.</p>
    <% } %>
  </div>
</div>
<%- include ("partials/footer.ejs") %>
